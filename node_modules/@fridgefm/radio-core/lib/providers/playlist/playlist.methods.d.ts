import type { TrackList } from './playlist.types';
export declare const SHUFFLE_METHODS: {
    rearrange: ({ to, from }: {
        to: number;
        from: number;
    }) => (arr: TrackList) => TrackList;
    randomShuffle: () => <T extends unknown>(arr: T[]) => T[];
};
