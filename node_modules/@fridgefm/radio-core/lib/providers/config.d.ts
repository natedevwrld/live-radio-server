export type Config = {
    responseHeaders: Record<string, string>;
    verbose: boolean;
    prebufferLength: number;
};
export declare const createConfig: (cfg: Partial<Config>) => Config;
export declare const DEFAULTS: {
    PREBUFFER_LENGTH: number;
};
