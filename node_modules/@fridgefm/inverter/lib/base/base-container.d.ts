import type { Container } from './base-container.types';
/**
 * Creates a base container where each token and provider have to be manually binded to each other
 */
export declare const createBaseContainer: (parentContainer?: Container | undefined) => Container;
