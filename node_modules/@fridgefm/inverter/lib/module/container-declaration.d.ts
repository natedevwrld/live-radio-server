import type { ContainerConfiguration } from './container.types';
import type { InjectableDeclaration } from './provider.types';
import type { Token, TokenDec } from '../base/token.types';
/**
 * Token for creating child containers, an injectable should be passed as the root of this container
 */
export declare const CHILD_DI_FACTORY_TOKEN: Token<(<A>(childProvider: InjectableDeclaration<Token<A>, readonly TokenDec<Token<any>>[]>) => () => A)>;
export declare const declareContainer: ({ providers, modules, parent, events }: ContainerConfiguration) => import("../base/base-container.types").Container;
